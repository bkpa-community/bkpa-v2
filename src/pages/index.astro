---
import BaseLayout from "../layouts/BaseLayout.astro";
import CategoryIcon from "../components/CategoryIcon.astro";
import { getCollection, getEntry } from "astro:content";
import { CATEGORIES, EMERGENCY_CATEGORIES } from "../lib/categories";

const home = (await getEntry("siteHome", "main"))!.data;
const [featured] = home.banners;

const articles = await getCollection("articles", (a) => !a.data.draft);
const countFor = (slug: string) => articles.filter((a) => a.id.startsWith(`${slug}/`)).length;
const photos = (await getCollection("gallery")).sort((a, b) => a.data.order - b.data.order);
const base = import.meta.env.BASE_URL.replace(/\/$/, "");

---

<BaseLayout title="Home" description="Detect early, protect kidney health — BKPA Foundation, Bangladesh's first comprehensive Bengali-language kidney health resource.">
  <!-- ============ HERO ============ -->
  <section class="pt-14 pb-12 lg:pt-20 lg:pb-16">
    <div class="mx-auto grid max-w-6xl items-center gap-11 px-6 md:px-10 lg:grid-cols-[1.05fr_.95fr] lg:gap-14">
      <div>
        <p class="font-mono text-[0.72rem] tracking-wider text-(--color-ink-soft) uppercase">
          <span class="i18n-bn bn">নন-প্রফিট চ্যারিটেবল অ্যাসোসিয়েশন · প্রতিষ্ঠা ২০১৫</span>
          <span class="i18n-en">Non-Profit Charitable Association · Est. 2015</span>
        </p>
        <h1 class="mt-3 font-display text-[clamp(2.15rem,5.4vw,3.4rem)] leading-[1.08] font-semibold text-balance text-(--color-ink)">
          {featured?.title ?? "Are your kidneys OK?"}
        </h1>
        {featured?.content && (
          <p class="mt-4 max-w-xl whitespace-pre-line text-[1.1rem] text-(--color-ink-soft)">{featured.content}</p>
        )}
        <p class="mt-2.5 font-display text-[1.05rem] text-(--color-accent-2) italic">
          <span class="i18n-bn bn">"কিডনি সেবায় জ্ঞানের ব্যবধান কমিয়ে আনা"</span>
          <span class="i18n-en">"Bridging the knowledge gap to better kidney care."</span>
        </p>
        <div class="mt-7 flex flex-wrap gap-3.5">
          <a class="inline-flex items-center gap-2 rounded-md bg-(--color-deep) px-5.5 py-3 text-sm font-semibold text-(--color-deep-ink) hover:brightness-110" href={`${base}/emergency-contacts/nephrology-doctors`}>
            <span class="i18n-bn bn">জরুরি সহায়তা</span><span class="i18n-en">Emergency Help</span>
          </a>
          <a class="inline-flex items-center gap-2 rounded-md border border-(--color-line) px-5.5 py-3 text-sm font-semibold hover:bg-(--color-surface-2)" href={`${base}/know-your-kidney/kidney-basic`}>
            <span class="i18n-bn bn">কিডনি সম্পর্কে জানুন</span><span class="i18n-en">Know Your Kidney</span>
          </a>
        </div>
      </div>

      <div class="relative mx-auto aspect-square w-full max-w-md">
        <svg viewBox="0 0 400 400" class="h-full w-full">
          <path d="M200 40c-70 0-120 62-120 138 0 96 48 172 120 172s120-76 120-172C320 102 270 40 200 40Z" fill="var(--color-surface-2)"></path>
          <path d="M200 40c-70 0-120 62-120 138 0 96 48 172 120 172" fill="none" stroke="var(--color-deep)" stroke-width="2" opacity=".5"></path>
          <path d="M162 150c0-30 20-52 44-52s44 26 44 58-18 46-40 46c-14 0-22-8-22-20 0-14 12-20 26-16" fill="none" stroke="var(--color-deep)" stroke-width="6" stroke-linecap="round"></path>
          <circle cx="200" cy="150" r="7" fill="var(--color-accent-2)"></circle>
        </svg>
        <div class="absolute bottom-[-1.1rem] left-1/2 flex w-max max-w-[92%] -translate-x-1/2 items-center gap-2.5 rounded-lg border border-(--color-line) bg-(--color-surface) px-4.5 py-3 shadow-xl">
          <span class="h-2.5 w-2.5 flex-none rounded-full bg-(--color-accent-2)"></span>
          <div>
            <strong class="block text-[0.82rem]"><span class="i18n-bn bn">সরকার নিবন্ধিত</span><span class="i18n-en">Government Registered</span></strong>
            <span class="text-[0.72rem] text-(--color-ink-soft)"><span class="i18n-bn bn">নন-প্রফিট প্রতিষ্ঠান</span><span class="i18n-en">Registered non-profit foundation</span></span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ NOTICE BOARD ============ -->
  {home.notice.enable && (
    <div class="border-y border-(--color-line) bg-(--color-deep) text-(--color-deep-ink)">
      <div class="mx-auto max-w-6xl px-6 py-5 md:px-10">
        <p class="font-semibold">{home.notice.title}</p>
        <p class="mt-1.5 max-w-3xl whitespace-pre-line text-sm opacity-90">{home.notice.content}</p>
      </div>
    </div>
  )}

  <!-- ============ FIND HELP / "I AM A..." ============ -->
  <section class="border-b border-(--color-line) bg-(--color-surface) py-14">
    <div class="mx-auto max-w-6xl px-6 md:px-10">
      <div class="max-w-xl">
        <p class="font-mono text-[0.72rem] tracking-wider text-(--color-ink-soft) uppercase"><span class="i18n-bn bn">সহায়তা খুঁজুন</span><span class="i18n-en">Find Help</span></p>
        <h2 class="mt-1.5 font-display text-[clamp(1.5rem,3vw,2rem)] font-semibold text-(--color-ink)"><span class="i18n-bn bn">আমরা কীভাবে সাহায্য করতে পারি?</span><span class="i18n-en">How can we help?</span></h2>
        <p class="mt-2 text-(--color-ink-soft)"><span class="i18n-bn bn-body">আপনি কে, তা বেছে নিন — আমরা আপনার জন্য উপযুক্ত তথ্য দেখাবো।</span><span class="i18n-en">Tell us who you are, and we'll guide you to the right information.</span></p>
      </div>

      <div class="mt-8 grid grid-cols-2 gap-3.5 lg:grid-cols-4">
        <a class="group flex flex-col items-start gap-3 rounded-lg border border-(--color-line) bg-(--color-paper) p-5 hover:border-(--color-deep)" href={`${base}/find-help?role=patient`}>
          <svg class="h-7 w-7 text-(--color-deep)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"></circle><path d="M4 21c0-4.4 3.6-8 8-8s8 3.6 8 8"></path></svg>
          <span class="text-sm font-semibold text-(--color-ink)">
            <span class="i18n-bn bn block">কিডনি রোগী</span><span class="i18n-en">Kidney Patient</span>
          </span>
          <span class="font-mono text-xs text-(--color-ink-soft) uppercase opacity-0 transition group-hover:opacity-100">
            <span class="i18n-bn bn">শুরু করুন</span><span class="i18n-en">Get Started</span> →
          </span>
        </a>
        <a class="group flex flex-col items-start gap-3 rounded-lg border border-(--color-line) bg-(--color-paper) p-5 hover:border-(--color-deep)" href={`${base}/find-help?role=caregiver`}>
          <svg class="h-7 w-7 text-(--color-deep)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="7" r="3.2"></circle><circle cx="17" cy="8.5" r="2.6"></circle><path d="M2.5 21c0-3.9 2.9-7 6-7s5.5 2.4 5.9 5.6"></path><path d="M13.5 15.5c2.4.1 4.5 2.2 5 5.5"></path></svg>
          <span class="text-sm font-semibold text-(--color-ink)">
            <span class="i18n-bn bn block">পরিবার / কেয়ারগিভার</span><span class="i18n-en">Family / Caregiver</span>
          </span>
          <span class="font-mono text-xs text-(--color-ink-soft) uppercase opacity-0 transition group-hover:opacity-100">
            <span class="i18n-bn bn">শুরু করুন</span><span class="i18n-en">Get Started</span> →
          </span>
        </a>
        <a class="group flex flex-col items-start gap-3 rounded-lg border border-(--color-line) bg-(--color-paper) p-5 hover:border-(--color-deep)" href={`${base}/find-help?role=at_risk`}>
          <svg class="h-7 w-7 text-(--color-deep)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 21s-7.5-4.6-9.7-9.3C.6 8.2 2.3 4.7 5.7 4.1c2-.4 3.9.5 5 2.1a1 1 0 0 0 1.5 0c1.1-1.6 3-2.5 5-2.1 3.4.6 5.1 4.1 3.4 7.6C19.5 16.4 12 21 12 21Z"></path><path d="M8 12h2.2l1.3-2.4 1.6 4.4L14.4 12H17"></path></svg>
          <span class="text-sm font-semibold text-(--color-ink)">
            <span class="i18n-bn bn block">ঝুঁকিপূর্ণ (ডায়াবেটিস/BP)</span><span class="i18n-en">At Risk (Diabetes/BP)</span>
          </span>
          <span class="font-mono text-xs text-(--color-ink-soft) uppercase opacity-0 transition group-hover:opacity-100">
            <span class="i18n-bn bn">শুরু করুন</span><span class="i18n-en">Get Started</span> →
          </span>
        </a>
        <a class="group flex flex-col items-start gap-3 rounded-lg border border-(--color-line) bg-(--color-paper) p-5 hover:border-(--color-deep)" href={`${base}/find-help?role=general`}>
          <svg class="h-7 w-7 text-(--color-deep)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5.5C4 4.7 4.7 4 5.5 4H12v16H5.5A1.5 1.5 0 0 1 4 18.5v-13Z"></path><path d="M20 5.5c0-.8-.7-1.5-1.5-1.5H12v16h6.5a1.5 1.5 0 0 0 1.5-1.5v-13Z"></path></svg>
          <span class="text-sm font-semibold text-(--color-ink)">
            <span class="i18n-bn bn block">সাধারণ জিজ্ঞাসা</span><span class="i18n-en">Just Learning</span>
          </span>
          <span class="font-mono text-xs text-(--color-ink-soft) uppercase opacity-0 transition group-hover:opacity-100">
            <span class="i18n-bn bn">শুরু করুন</span><span class="i18n-en">Get Started</span> →
          </span>
        </a>
      </div>
    </div>
  </section>

  <!-- ============ VITALS ============ -->
  <div class="border-b border-(--color-line) bg-(--color-surface)">
    <div class="mx-auto grid max-w-6xl grid-cols-1 sm:grid-cols-3">
      <div class="border-b border-(--color-line) px-6 py-5 sm:border-r sm:border-b-0 md:px-10">
        <div class="font-mono text-[1.3rem] font-semibold text-(--color-ink) tabular-nums">2015</div>
        <div class="mt-1 text-sm text-(--color-ink-soft)"><span class="i18n-bn bn">এপ্রিল মাসে প্রতিষ্ঠিত</span><span class="i18n-en">Founded in April</span></div>
      </div>
      <div class="border-b border-(--color-line) px-6 py-5 sm:border-r sm:border-b-0 md:px-10">
        <div class="font-mono text-[1.1rem] font-semibold text-(--color-ink)"><span class="i18n-bn bn">প্রথম বাংলা রিসোর্স</span><span class="i18n-en">First of its kind</span></div>
        <div class="mt-1 text-sm text-(--color-ink-soft)"><span class="i18n-bn bn">বাংলাদেশের প্রথম সম্পূর্ণ বাংলা কিডনি স্বাস্থ্য রিসোর্স</span><span class="i18n-en">Bangladesh's first full Bengali-language kidney resource</span></div>
      </div>
      <div class="px-6 py-5 md:px-10">
        <div class="font-mono text-[1.3rem] font-semibold text-(--color-ink) tabular-nums">{articles.length}+</div>
        <div class="mt-1 text-sm text-(--color-ink-soft)"><span class="i18n-bn bn">শিক্ষামূলক আর্টিকেল</span><span class="i18n-en">Educational articles</span></div>
      </div>
    </div>
  </div>

  <!-- ============ KNOW YOUR KIDNEY ============ -->
  <section class="py-14" id="topics">
    <div class="mx-auto max-w-6xl px-6 md:px-10">
      <div class="mb-8 max-w-xl">
        <p class="font-mono text-[0.72rem] tracking-wider text-(--color-ink-soft) uppercase"><span class="i18n-bn bn">শিক্ষামূলক রিসোর্স</span><span class="i18n-en">Educational Resource</span></p>
        <h2 class="mt-1.5 font-display text-[clamp(1.5rem,3vw,2rem)] font-semibold text-(--color-ink)"><span class="i18n-bn bn">আপনার কিডনি সম্পর্কে জানুন</span><span class="i18n-en">Know Your Kidney</span></h2>
        <p class="mt-2 text-(--color-ink-soft)"><span class="i18n-bn bn-body">কিডনির গঠন থেকে ডায়ালাইসিস ও প্রতিস্থাপন পর্যন্ত — প্রতিটি বিষয় সহজ বাংলায় ব্যাখ্যা করা।</span><span class="i18n-en">From kidney basics to dialysis and transplantation — every topic explained in plain Bengali and English.</span></p>
      </div>
      <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">
        {CATEGORIES.map((c) => (
          <a class="flex min-h-[8.5rem] flex-col justify-between gap-3 rounded-lg border border-(--color-line) bg-(--color-surface) p-5 hover:border-(--color-deep)" href={`${base}/know-your-kidney/${c.slug}`}>
            <div>
              <CategoryIcon slug={c.slug} class="h-6 w-6 text-(--color-deep)" />
              <p class="mt-2.5 text-sm font-semibold leading-tight">
                <span class="i18n-en">{c.labelEn}</span>
                <span class="i18n-bn bn block">{c.labelBn}</span>
              </p>
            </div>
            <span class="font-mono text-xs text-(--color-ink-soft) tabular-nums">{countFor(c.slug)} <span class="i18n-bn bn">টি আর্টিকেল</span><span class="i18n-en">articles</span></span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ EMERGENCY ============ -->
  <section class="bg-(--color-deep) py-14 text-(--color-deep-ink)" id="emergency">
    <div class="mx-auto max-w-6xl px-6 md:px-10">
      <div class="mb-8 max-w-xl">
        <p class="font-mono text-[0.72rem] tracking-wider opacity-75 uppercase"><span class="i18n-bn bn">তাৎক্ষণিক প্রয়োজনে</span><span class="i18n-en">When you need it now</span></p>
        <h2 class="mt-1.5 font-display text-[clamp(1.5rem,3vw,2rem)] font-semibold"><span class="i18n-bn bn">জরুরি যোগাযোগ</span><span class="i18n-en">Emergency Contacts</span></h2>
        <p class="mt-2 opacity-90"><span class="i18n-bn bn-body">নেফ্রোলজিস্ট থেকে ব্লাড ব্যাংক পর্যন্ত — ক্যাটাগরি অনুযায়ী দ্রুত খুঁজে নিন।</span><span class="i18n-en">From nephrologists to blood banks — find the right contact by category, fast.</span></p>
      </div>
      <div class="grid grid-cols-2 gap-3.5 sm:grid-cols-3">
        {EMERGENCY_CATEGORIES.map((c) => (
          <a class="rounded-lg border border-[color-mix(in_srgb,var(--color-deep-ink)_30%,transparent)] bg-[color-mix(in_srgb,var(--color-deep-ink)_12%,transparent)] p-4.5 hover:bg-[color-mix(in_srgb,var(--color-deep-ink)_20%,transparent)]" href={`${base}/emergency-contacts/${c.slug}`}>
            <span class="block text-sm font-semibold"><span class="i18n-bn bn">{c.labelBn}</span><span class="i18n-en">{c.labelEn}</span></span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ GALLERY ============ -->
  {photos.length > 0 && (
    <section class="py-14">
      <div class="mx-auto max-w-6xl px-6 md:px-10">
        <div class="mb-8 max-w-xl">
          <p class="font-mono text-[0.72rem] tracking-wider text-(--color-ink-soft) uppercase"><span class="i18n-bn bn">ছবিঘর</span><span class="i18n-en">Gallery</span></p>
          <h2 class="mt-1.5 font-display text-[clamp(1.5rem,3vw,2rem)] font-semibold text-(--color-ink)"><span class="i18n-bn bn">আমাদের কার্যক্রম</span><span class="i18n-en">Our Activities</span></h2>
        </div>
        <div class="grid grid-cols-2 gap-3.5 sm:grid-cols-3 lg:grid-cols-4">
          {photos.map((p) => (
            <figure class="group overflow-hidden rounded-lg border border-(--color-line) bg-(--color-surface)">
              <div class="aspect-square overflow-hidden">
                <img
                  class="h-full w-full object-cover transition group-hover:scale-105"
                  src={encodeURI(`${base}${p.data.image}`)}
                  alt={p.data.caption}
                  loading="lazy"
                  width="400"
                  height="400"
                />
              </div>
              {p.data.caption && <figcaption class="p-2.5 text-xs text-(--color-ink-soft)">{p.data.caption}</figcaption>}
            </figure>
          ))}
        </div>
      </div>
    </section>
  )}

</BaseLayout>
